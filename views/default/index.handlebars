{{> default/slider }}
<section class="section">
    <div class="row">
        <div class="col-md-10">
            <div class="row">
                {{#each posts}}
                <div class="col-md-3">
                    <a href="#" class="post-card-wrapper">
                        <div class="post-card">
                            <img src="{{file}}" alt="Blog image" class="img-fluid post-image">
                            <h3 class="post-title">{{title}}</h1>
                                <small class="post-date">Created {{creationDate}} by Admin</small>
                                <p>{{description}}</p>
                        </div>
                    </a>
                </div>
                {{/each}}
            </div>
        </div>
        <div class="col-md-2">
            <h3>Kategorije</h3>
            <ul>
                {{#each categories}}
                <li><a href="#">{{title}}</a></li>
                {{/each}}
            </ul>
        </div>
    </div>
</section>